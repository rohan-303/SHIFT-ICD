# SHIFT-MAP v1.3 TEST-exposure disclosure

Step 7.3 is not a pristine first-look TEST experiment.

The Track A TEST partition was previously observed:

1. under the defective Step 7 evaluator; and
2. through diagnostic corrected L1 replay in Step 7.2.

Accordingly, Step 7.3 configuration selection is restricted to TRAIN and forward-stratified DEV. No TEST metric, lexical TEST slice, family-held-out TEST result, or backward TEST result may select the loss, negative strategy, learning rate, epoch, or seed.

Step 7.3 uses the repaired direction-scoped evaluator v2.0 and preserves the original benchmark v1.0, canonical schema v1.0, train/DEV/TEST membership, and frozen BioLORD revision. Final TEST evaluation occurs only after the configuration and TEST lock are frozen.
